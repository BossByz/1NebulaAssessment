1) Both div's will not be the same colour. The first div will be red, while the second div will be orange because its ID will override the shared properties of its class.

2) document.getElementById("firstDiv").style.background = "pink";

3) document.getElementById("secondDiv").classList.add("yellow-card");